class Crop {
  // TODO fuse with vegetable model
  final String type;
  final int amount;
  final int area;
  final int cost;
  final String unit;

  Crop({this.type, this.amount, this.area, this.cost, this.unit});
}
